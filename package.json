{
  "name": "purescript-free-turtle",
  "version": "0.1.0",
  "description": "Turtle graphics language with a free monad Canvas interpreter, for educational purposes.",
  "main": "",
  "private": true,
  "author": "Erik Post",
  "license": "MIT",
  "scripts": {
    "postinstall": "npm run spago-install",
    "build": "spago build",
    "bundle": "spago bundle --platform browser --bundle-type module --module Main --outfile html/index.js --minify",
    "spago-install": "spago install"
  },
  "dependencies": {
    "purescript": "^0.15.16",
    "spago": "^1.0.4"
  }
}
