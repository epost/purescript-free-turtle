{
  "name": "purescript-free-turtle",
  "version": "0.1.0",
  "description": "Turtle graphics language with a free monad Canvas interpreter, for educational purposes.",
  "main": "",
  "private": true,
  "author": "Erik Post",
  "license": "MIT",
  "scripts": {
    "postinstall": "spago install",
    "build": "spago build",
    "bundle": "spago bundle --platform browser --bundle-type main --module Main --outfile html/index.js --minify",
    "serve": "npx serve ./html"
  },
  "devDependencies": {
    "purescript": "^0.15.16",
    "spago": "^1.0.4",
    "esbuild": "^0.28.2",
    "serve": "^14.2.6"
  }
}
